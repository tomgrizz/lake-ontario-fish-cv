# Inference

DETR + BoT-SORT inference pipeline for fish detection, tracking, and species classification.

## Outputs

Per-detection records (parquet) and per-track summaries (SQLite). Output schema is
documented in `../docs/output_schema.md` (to be written).

## Configuration

BoT-SORT and inference settings live in `config/`.
